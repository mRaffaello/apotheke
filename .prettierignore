**/node_modules/
**/dist/
**/.next/
**/out/
**/.source/
**/.turbo/
packages/apotheke/tests/e2e/repos/
pnpm-lock.yaml
**/next-env.d.ts

# Prettier's MDX printer dedents the second and later paragraphs of a JSX
# component's children out to column 0 and leaves a blank line before the
# closing tag, which can push that content out of the component entirely.
# (Unrelated to apotheke: the plugin now skips markdown/mdx snippets — see
# isDocumentSnippet in packages/apotheke/index.ts.)
**/*.mdx
